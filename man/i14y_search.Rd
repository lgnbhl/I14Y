% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/i14y_search.R
\name{i14y_search}
\alias{i14y_search}
\title{Search the I14Y catalog}
\usage{
i14y_search(
  query = NULL,
  language = "de",
  types = NULL,
  accessRights = NULL,
  businessEvents = NULL,
  conceptValueTypes = NULL,
  formats = NULL,
  publicationLevels = NULL,
  publicationLevelProposals = NULL,
  lifeEvents = NULL,
  publishers = NULL,
  registrationStatuses = NULL,
  registrationStatusProposals = NULL,
  structure = NULL,
  themes = NULL,
  page = NULL,
  pageSize = NULL
)
}
\arguments{
\item{query}{string. The search query.}

\item{language}{string. The language to use for the search ("de", "en",
"fr", "it", "rm"). Default "de".}

\item{types}{character vector. Filter by resource types. One or more of
"Dataset", "DataService", "PublicService", "Concept", "MappingTable".}

\item{accessRights}{character vector. Filter by access rights.}

\item{businessEvents}{character vector. Filter by business events.}

\item{conceptValueTypes}{character vector. Filter by concept value type. One
or more of "CodeList", "Date", "Numeric", "String".}

\item{formats}{character vector. Filter by distribution formats.}

\item{publicationLevels}{character vector. Filter by publication level
("Internal", "Public").}

\item{publicationLevelProposals}{character vector. Filter by proposed
publication level ("Internal", "Public").}

\item{lifeEvents}{character vector. Filter by life events.}

\item{publishers}{character vector. Filter by publisher identifiers.}

\item{registrationStatuses}{character vector. Filter by registration status.}

\item{registrationStatusProposals}{character vector. Filter by proposed
registration status.}

\item{structure}{string. One of "WithStructure", "WithoutStructure".}

\item{themes}{character vector. Filter by theme codes.}

\item{page}{integer. The number of the result page to return.}

\item{pageSize}{integer. The size of each result page.}
}
\value{
A tibble of search results. `NULL` when offline.
}
\description{
Calls the I14Y public API endpoint \verb{/search}. This is the faceted search
over all resource types (datasets, data services, public services, concepts,
mapping tables) and replaces the previous private `input-backend` host that
powered `i14y_search_catalog()` and `i14y_search_concept()` in earlier
releases.
}
\examples{
\donttest{
i14y_search(query = "noga", language = "en")
}
}
